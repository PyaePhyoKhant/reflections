What happens when you initialize a repository? Why do you need to do it?
New .git folder is created. It is needed to make current folder as repository
so that git features will work.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
It is the medium between the working directory and the repository.
It offer flexibility of what to commit.

How can you use the staging area to make sure you have one commit per logical
change?
By adding necessary files for one logical change to the stagin area.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
Branches would be helpful when I want to add changes but I do not want master branch to change yet.
For e.g, experimental changes or new features.

How do the diagrams help you visualize the branch structure?
It make it easy to visualize connetion between commits and branches.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
The result of merging two branches is new commit. Because it is easier to visualize

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
Git's automatic merging save time. Git's automatic merging consume time but
we can exact merging we want.
